// Remembers the chosen mode while moving from MainMenu to SampleScene.
// A static class does not need to be attached to a GameObject.
public static class GameModeSettings
{
    public static bool isPracticeMode;
}
